
spring:
  datasource:
    url: jdbc:mysql://database-2.cwqtamls05ky.ap-northeast-2.rds.amazonaws.com:3306/samsungDB
    username: admin
    password: adminadmin
    driver-class-name: com.mysql.cj.jdbc.Driver
  servlet:
    multipart:
      maxFileSize: 100MB
      maxRequestSize: 200MB
  jpa:
    open-in-view: false
    show-sql: true
    database-platform: org.hibernate.dialect.MariaDB106Dialect
    hibernate:
      format_sql: true
      ddl-auto: update
  main:
    allow-bean-definition-overriding: true

logging:
  level:
    org:
      hibernate:
        SQL: DEBUG
        type:
          descriptor:
            sql:
              BasicBinder: TRACE
